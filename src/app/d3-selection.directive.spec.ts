import { D3SelectionDirective } from './d3-selection.directive';

describe('D3SelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new D3SelectionDirective();
    expect(directive).toBeTruthy();
  });
});
